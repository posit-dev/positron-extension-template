name: Positron API Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          cache: "npm"
      - run: npm ci
      # Downloads a real Positron build and runs src/test/positron/ inside it.
      - uses: posit-dev/setup-positron@main
        with:
          positron-channel: stable
          run: npm run test-positron
